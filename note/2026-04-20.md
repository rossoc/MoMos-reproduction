title: 20/04
layout: page
author: Carlo Rosso
date: 2026-04-20
---

1. Compute compression metrics:

```tex
\paragraph{Relative Algorithmic Compression (RAC).} 


We define the relative algorithmic compression (RAC) rate, $r_\mathcal{K}$ as the ratio of the description length for the baseline model (in Eq.~\eqref{eq:kc_trivial}) and that for the MoMos construction obtained from the complexity bound (in Eq.~\ref{eq:momos-comp}),
\begin{equation}
r_\mathcal{K}
= \frac{\mathcal{K}(\W)}{\mathcal{K}(\What)}
\approx \frac{nq}{ksq + m\ceil{\log_2 k}}.
\label{eq:rac}
\end{equation}
We ignore the constants and hence the last quantity is an approximation. For QAT, we report the RAC rate as $32/q$ relative to the length of $\mathrm{FP32}$ model.

\paragraph{BDM Complexity Ratio.} We also compare the algorithmic complexity of the trained neural network weights, $\W^*$, using BDM complexity denoted $\mathcal{K}_B$ relative to the BDM value of randomly initialized weights $\W_0$. To do this, we binarize the weights and report the BDM complexity ratio as: 
\begin{equation}
    r_\mathrm{BDM} = \frac{\mathcal{K}_B(\W^*)}{\mathcal{K}_B(\W_0)}.
\end{equation}
```

2. Compute distribution of the weights:
    - there are two models in artifacts
    1. count how many times each motif is repeated in the model:
        - sort the count by size: 0 the biggest, 1 the smallest
        - plot
    2. count how many times each motif is repeated in one layer:
        - don't change the order of the motifs
        - plot the counts (index on the x, count on the y)
        - different "lines" for each layer
    3. compute mean/standard deviation of the motifs
    4. 2D PCA to motifs, compute k-clusters, show both (all the motifs and the clusters in a plot)
