title: 27/07
layout: page
author: Carlo Rosso
date: 2026-07-27
---

## Introduction

$$
K(s) = min{p|UTM(p) = s}
$$

If you relax minimum constraint you can compute it, there seem to be a
connection with causality.

## Algorithmic Ambiguity

Ambiguity (Knightian uncertainty) is connected to safety.

Intuition: Novelty
ML: OOD
Theory: non-stationarity

AI safety: AI agents should be cautious about (mis)interpreting ambiguous
requests without being paralyzed by indecision.

The future is ambiguous at $x$ iff $M | x \approx M^x$.
Under ambiguity we still remember the past, but we do not (probabilistically)
condition on it (put $x$ on one of the UTM's tapes).

$$ m(x) = \sum_{p|UTM(p)=x} 2^(-|p|) = M^(-K(x)) $$

---

Ambiguity: many explanations are available?
If many explanations are available, does it mean that there is a shorter one?

## Interestingness

Interestingness as an Inductive Heuristic for Future Compression Progress.

What is interesting? Something that the Shannon entropy is not able to predict.
What if you store it for repetitions?

> Interestingness is the expected future compression progress under further
> engagement.

Maybe it's hard to predict when the next interesting pattern will be discovered.
But is it easy to predict whether a pattern is interesting, without running it?
Or, how can we search for interesting patterns?

The complexity of an object is related to the run time to generate it.
Based on how fast a compression is, is connected to the probability to find a
slower, but shorter description.

---

- Similarly to prime numbers, you can search nearby multiple of 6 (6a +- 1).
- If you memorize the exact state of the UTM, you can use dynamic programming to
  store it. Is it really feasible? This become very similar to a chess game.

## OOD generalization and model size selection

What is Volume?

If many explanations are available, does it mean that there is a shorter one?
Or, what is the best one?
> You cannot generalize while being uncertain.

Isn't the best way to pick the shorter one?

## Algorithmic compression via Pretrained NN

Memory-Based Meta-Learning (MBML)

---

Volume vs Complexity in Kolmogorov

## Job-Less Utopia

### Macroeconomies in the Age of AGI

If robots replace workers the economy doesn't have to shrink.

---

## Kernel methods to bridge AIT and Machine Learning

- KC Kernel
- Solomonoff Kernel


